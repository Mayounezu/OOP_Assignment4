<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
          https://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>hw4</id>
    <formats>
        <format>jar</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <!-- Runtime dependency classes (JUnit is test-scoped, excluded here) -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>/</outputDirectory>
            <useProjectArtifact>false</useProjectArtifact>
            <unpack>true</unpack>
            <scope>runtime</scope>
        </dependencySet>
    </dependencySets>

    <fileSets>
        <!-- Compiled main classes -->
        <fileSet>
            <directory>${project.build.outputDirectory}</directory>
            <outputDirectory>/</outputDirectory>
        </fileSet>

        <!-- Compiled test classes -->
        <fileSet>
            <directory>${project.build.testOutputDirectory}</directory>
            <outputDirectory>/test-classes</outputDirectory>
        </fileSet>

        <!-- Main source code -->
        <fileSet>
            <directory>${project.basedir}/src/main/java</directory>
            <outputDirectory>/sources/main</outputDirectory>
            <includes>
                <include>**/*.java</include>
            </includes>
        </fileSet>

        <!-- Test source code -->
        <fileSet>
            <directory>${project.basedir}/src/test/java</directory>
            <outputDirectory>/sources/test</outputDirectory>
            <includes>
                <include>**/*.java</include>
            </includes>
        </fileSet>
    </fileSets>
</assembly>
